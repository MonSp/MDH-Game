针对你给出的代码，你需要解决的核心逻辑其实是**将判定坐标从“世界原点”改为“玩家位置”**。你目前的遮挡判定逻辑是判断摄像机位置到**坐标原点 (0,0)** 之间有没有建筑，所以当玩家移动后，建筑就会错误地透明化。

根本解决方案是：**从摄像机位置发射一条射线，检测其是否指向玩家当前的位置**。如果射线在到达玩家之前碰到了建筑/树木，就将该建筑/树木变透明。

下面为你提供一个完整的、更稳定的改进方案，直接修改你的 `BuildingWorld.tsx`，并配合 `BuildingGeometry.tsx` 做一点点微调。

### 1. 修改 `BuildingWorld.tsx` (核心逻辑替换)

将原来的 `rayHitsAABB` 替换为基于 Three.js 原生 `Box3` 的射线检测。同时利用 `useFrame` 在每一帧实时检测，并用 `ref` 存储建筑对象。

```tsx
// BuildingWorld.tsx
import React, { useEffect, useRef, useState, useMemo } from 'react';
import { useThree, useFrame } from '@react-three/fiber';
import * as THREE from 'three';
import { BuildingGeometry } from './BuildingGeometry';
import { useBuildingStore } from './BuildingStore';

interface BuildingWorldProps {
  playerX: number;
  playerY: number;
  viewRadius: number;
}

export const BuildingWorld = React.memo(({
  playerX,
  playerY,
  viewRadius,
}: BuildingWorldProps) => {
  const { camera } = useThree();
  const buildings = useBuildingStore((s) => s.buildings);
  const currentBuildingId = useBuildingStore((s) => s.currentBuildingId);
  const enterBuilding = useBuildingStore((s) => s.enterBuilding);
  const exitBuilding = useBuildingStore((s) => s.exitBuilding);

  // 使用 ref 存储所有建筑的实际 Mesh 组 (用于射线检测)
  const buildingRefs = useRef<Map<string, THREE.Group>>(new Map());
  const [ghostedIds, setGhostedIds] = useState<Set<string>>(new Set());

  // 1. 玩家进出建筑逻辑 (保持不变)
  useEffect(() => {
    let foundInside = false;
    for (const b of buildings) {
      const hw = b.def.compoundWidth / 2;
      const hd = b.def.compoundDepth / 2;
      const localX = playerX - b.worldX;
      const localY = playerY - b.worldY;
      if (localX >= -hw && localX <= hw && localY >= -hd && localY <= hd) {
        foundInside = true;
        if (currentBuildingId !== b.id) {
          enterBuilding(b.id);
        }
        break;
      }
    }
    if (!foundInside && currentBuildingId) {
      exitBuilding();
    }
  }, [playerX, playerY, buildings, currentBuildingId, enterBuilding, exitBuilding]);

  // 2. 构建射线检测器
  const raycaster = useMemo(() => new THREE.Raycaster(), []);
  const _direction = useMemo(() => new THREE.Vector3(), []);
  const _origin = useMemo(() => new THREE.Vector3(), []);

  // 3. 每一帧检测摄像机到玩家的遮挡
  useFrame(() => {
    // 3.1 准备射线数据
    const camPos = camera.position;
    // 这里的 playerY 在二维平面中对应 Three.js 的 Z 轴，如果你的 y 是 height 请调整
    _origin.set(camPos.x, camPos.z, 0); 
    // 方向向量 = 玩家位置 (世界坐标) - 相机位置 (忽略高度差，进行平面探测)
    _direction.set(playerX - camPos.x, playerY - camPos.z, 0).normalize();

    const maxDistance = 200; // 设定最大检测距离，防止检测到超远物体

    // 3.2 收集所有可见建筑的 Mesh 并进行检测
    const targetObjects: THREE.Mesh[] = [];
    const newGhosted = new Set<string>();

    // 只检测距离玩家较近的建筑 (性能优化)
    for (const b of buildings) {
      const distToPlayer = Math.sqrt((b.worldX - playerX)**2 + (b.worldY - playerY)**2);
      if (distToPlayer > viewRadius) continue;

      const group = buildingRefs.current.get(b.id);
      if (!group) continue;

      // 获取该建筑组内的所有 Mesh
      group.children.forEach(child => {
        if (child.type === 'Group') {
          child.children.forEach(grandChild => {
            if ((grandChild as THREE.Mesh).isMesh) {
              targetObjects.push(grandChild as THREE.Mesh);
            }
          });
        } else if ((child as THREE.Mesh).isMesh) {
          targetObjects.push(child as THREE.Mesh);
        }
      });
    }

    if (targetObjects.length === 0) {
      if (ghostedIds.size > 0) setGhostedIds(new Set());
      return;
    }

    // 3.3 执行射线检测
    // 重要：如果玩家距离相机非常近 (比如进入建筑内部)，射线可能直接射到外部围墙。
    // 这里只是基础版实现
    raycaster.set(_origin, _direction);
    raycaster.far = maxDistance;
    const intersects = raycaster.intersectObjects(targetObjects, false); // 不深挖子对象

    // 3.4 计算玩家到相机的距离
    const distToPlayer = Math.sqrt((playerX - camPos.x)**2 + (playerY - camPos.z)**2);

    for (const hit of intersects) {
      // 如果碰撞点距离小于玩家距离，说明玩家视线被遮挡
      if (hit.distance < distToPlayer) {
        // 找到这个 Mesh 所属的建筑 ID (通过遍历 buildings 匹配)
        // 这里需要高效匹配。建议使用 userData 或自定义属性，比如：
        // mesh.userData.buildingId = b.id; 
        // 为了简化演示，这里采用一种稍微慢一点的查找方式：
        let foundBuildingId = null;
        for (const b of buildings) {
           const group = buildingRefs.current.get(b.id);
           if (group && hit.object.parent === group) {
             foundBuildingId = b.id;
             break;
           }
        }
        if (foundBuildingId) {
          newGhosted.add(foundBuildingId);
        }
      }
    }

    // 3.5 更新 ghostedIds
    setGhostedIds(prev => {
      if (prev.size === newGhosted.size && [...prev].every(id => newGhosted.has(id))) return prev;
      return newGhosted;
    });
  });

  return (
    <group>
      {buildings.map((b) => {
        const inside = currentBuildingId === b.id;
        const relX = b.worldX - playerX;
        const relY = b.worldY - playerY;
        const dist = Math.sqrt(relX * relX + relY * relY);
        if (!inside && dist > viewRadius + b.def.compoundWidth * 0.5) return null;

        return (
          <BuildingGeometry
            key={b.id}
            building={b.def}
            playerInside={inside}
            position={[relX, 0, relY]}
            ghostMode={ghostedIds.has(b.id)}
            ref={(group) => {
              if (group) buildingRefs.current.set(b.id, group);
              else buildingRefs.current.delete(b.id);
            }} // 这里需要给 BuildingGeometry 加上 forwardRef
          />
        );
      })}
    </group>
  );
});
```

### 2. 微调 `BuildingGeometry.tsx`

需要加上 `forwardRef` 让父组件能获取到它的 Group 实例，使得上述代码中的 `buildingRefs` 能正常工作。

```tsx
// BuildingGeometry.tsx 开头部分修改
import React, { useMemo, useRef, forwardRef } from 'react';
// ...其他 import

// 在 function 定义时加上 forwardRef
export const BuildingGeometry = React.memo(forwardRef<THREE.Group, BuildingGeometryProps>(({
  building,
  playerInside = false,
  position = [0, 0, 0],
  ghostMode = false,
}, ref) => {
  // ...中间逻辑不变

  // const ghostOpacityRef = useRef(ghostMode ? 0.35 : 1); 
  // 注意：刚才 useFrame 的逻辑放在外面，这里只需要渲染。但为了符合你原有的设计，
  // 也可以保留 ghostOpacity 插值。但为了性能，建议只在父组件计算，直接传进来。

  // 保存 ref: 在最外层的 group 上加上 ref
  return (
    <group ref={ref} position={position}>
      {/* ...其他网格 */}
    </group>
  );
}));
```

### 3. 关于“树木”的处理方案（重要补充）

你代码中明确提到 `trees`。如果树木不是包含在 `BuildingGeometry` 里，而是**独立的三维模型**，你需要把它们加入遮挡检测列表。

**最佳做法：** 专门建立一个 `TreeWorld` 组件，该组件负责生成树木，并把它们的 Mesh 存入一个全局可访问的 `treeRefs`。然后在 `BuildingWorld.tsx` 中的 `useFrame` 里，把 `treeRefs` 里的 Mesh 也加入到 `targetObjects` 中。

### 4. 透明化性能与视觉优化建议

1.  **平滑过渡（防闪烁）**：
    不用 `setGhostedIds` 立即变更。可以在 `BuildingGeometry` 里使用 `useRef` 存储 `localOpacity`，并在 `useFrame` 中做插值。但是**每个组件都跑 useFrame 很耗性能**。建议在 `BuildingWorld` 的 `useFrame` 里通过更新 `ghostedIds` 来一次性计算透明度值。

2.  **Two-pass 渲染（高级）**：
    简单来说，**先渲染不透明物体，再渲染透明半透明物体**。目前你的代码中只要 `ghostMode` 就会把透明度改为 `0.2`，并设置 `transparent: true`。
    Three.js 默认的渲染器会处理这个顺序。但要注意：
    *   如果某建筑是 `ghosted`，它一般会排在所有不透明建筑后面渲染。
    *   如果你发现半透明的墙壁遮挡了后面的不透明建筑，确保半透明材质的 `depthWrite` 设置为 `false`（你已经在 `RoadStrip` 里这么做了，在 `BuildingGeometry` 里的主体建筑也应该加上 `depthWrite: !isGhost`，但注意**不透明建筑**需要 `depthWrite: true`，否则显示错误）。

3.  **射线检测优化（核心技巧）**：
    **不要**像上面的样本那样遍历建筑物里的所有子物体来构建 `targetObjects`。推荐做法是**给 Group 添加 bounding box**。
    使用 `raycaster.intersectObject(buildingGroup, true)` 并传入 `recursive=true`，它也能自动检测子元素，但你最好为 `buildingGroup` 设置一个精确的 `Box3`，或者把整个建筑作为单个 Mesh 处理（或者用 `THREE.BoxHelper` 计算物理包围盒）。

**总结要点**：你把 `rayHitsAABB` 的逻辑弄反了，它是在检测视线是否穿过**(0,0)原点**，而不是玩家。把射线改为 `摄像机位置 -> 玩家位置` 即可修复视觉Bug。性能方面，**不要每帧对几百个 Mesh 进行检测**，尽量只在玩家周围 `viewRadius` 区域内检测。