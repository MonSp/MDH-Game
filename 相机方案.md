理解你的挫败感——明明照着业界标准做法改了，中键还是触发浏览器的“自动滚动”光标。这种情况通常不是逻辑方向错误，而是**执行细节或环境干扰**导致。

我们重新梳理一下，把最小可行方案和诊断步骤都给你。

---

## 核心原理再确认
`setPointerCapture` 本身就能让浏览器**不再将指针事件解释为手势**（包括自动滚动）。但前提是：

1. **必须在指针按下时立即调用**，且在同一个事件循环；
2. **事件源必须支持 Pointer Capture**（canvas 支持）；
3. **不能有其它元素或监听器抢先处理**；
4. **某些浏览器需要同步的 `preventDefault`** 来配合。

如果以上都做到了却仍失效，极可能是**监听器挂载位置、第三个参数 `passive`、或者有全局拦截器**在作祟。

---

## 最小可复现测试（复制即用）
先用这段绝对精简的代码测试中键旋转，不依赖任何其它框架，直接验证浏览器行为。

```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { margin: 0; overflow: hidden; }
    canvas { display: block; touch-action: none; }
  </style>
</head>
<body>
  <canvas id="c"></canvas>
  <script type="importmap">
    { "imports": { "three": "https://unpkg.com/three@0.160.0/build/three.module.js" } }
  </script>
  <script type="module">
    import * as THREE from 'three';

    const canvas = document.getElementById('c');
    const renderer = new THREE.WebGLRenderer({ canvas });
    renderer.setSize(window.innerWidth, window.innerHeight);
    const scene = new THREE.Scene();
    scene.add(new THREE.AmbientLight(0xffffff));
    const camera = new THREE.PerspectiveCamera(45, window.innerWidth / window.innerHeight, 0.1, 100);
    camera.position.set(3, 3, 3);
    camera.lookAt(0, 0, 0);
    const box = new THREE.Mesh(new THREE.BoxGeometry(), new THREE.MeshNormalMaterial());
    scene.add(box);

    // ========== 测试控制器 ==========
    let isDragging = false;
    let lastX, lastY;
    const spherical = new THREE.Spherical().setFromVector3(
      new THREE.Vector3().copy(camera.position).sub(new THREE.Vector3(0,0,0))
    );

    canvas.addEventListener('pointerdown', (e) => {
      // 对所有 button 都 preventDefault（包括中键）
      e.preventDefault();
      canvas.setPointerCapture(e.pointerId);
      isDragging = true;
      lastX = e.clientX;
      lastY = e.clientY;
    });

    // 移出 canvas 也能拖拽 —— 在 window 上监听
    window.addEventListener('pointermove', (e) => {
      if (!isDragging) return;
      e.preventDefault();
      const dx = e.clientX - lastX;
      const dy = e.clientY - lastY;
      lastX = e.clientX;
      lastY = e.clientY;

      spherical.theta -= dx * 0.005;
      spherical.phi   -= dy * 0.005;
      spherical.phi = Math.max(0.1, Math.min(Math.PI - 0.1, spherical.phi));

      camera.position.setFromSpherical(spherical).add(new THREE.Vector3(0,0,0));
      camera.lookAt(0, 0, 0);
    });

    window.addEventListener('pointerup', (e) => {
      if (!isDragging) return;
      isDragging = false;
      canvas.releasePointerCapture(e.pointerId);
    });

    // 缩放
    canvas.addEventListener('wheel', (e) => {
      e.preventDefault();
      spherical.radius += e.deltaY * 0.01;
      spherical.radius = Math.max(0.5, Math.min(10, spherical.radius));
      camera.position.setFromSpherical(spherical).add(new THREE.Vector3(0,0,0));
      camera.lookAt(0, 0, 0);
    }, { passive: false });

    function animate() {
      requestAnimationFrame(animate);
      renderer.render(scene, camera);
    }
    animate();
  </script>
</body>
</html>
```

**用这个页面测试：**
- 按鼠标中键拖拽，看是否还会出现“自动滚动”光标。  
  正常情况下**应该不会出现**，只会旋转立方体。
- 如果这个页面工作，说明你的实现里有其他因素干扰。  
  如果依然不行，问题在浏览器或系统层面。

---

## 常见导致失败的隐藏原因及修复

### 1. 事件监听器未设置 `{ passive: false }` （尤其是 Chrome 73+ 的 `wheel` 和 `touchstart`）
虽然 `pointerdown` 默认不是 passive，但 `wheel` 是。如果你在 `pointerdown` 里调用了 `setPointerCapture`，而之前某个全局监听器阻止了事件传播，也会失效。

**排查：**
```js
// 确保 DOM 最顶层没有 passive 监听覆盖
window.addEventListener('pointerdown', (e) => {
  console.log('global pointerdown', e.defaultPrevented);
});
```

### 2. 在 **触摸设备** 上测试中键？  
中键是鼠标独有，触摸屏不存在 `button === 1`。请确认设备。

### 3. 第三方浏览器扩展或无障碍工具拦截了 Pointer 事件  
禁用所有扩展（尤其是鼠标手势、拖拽相关）再试。

### 4. `setPointerCapture` 可能因为 canvas 尺寸为 0 或被隐藏而失败  
检查 canvas 是否有实际尺寸，且不在 `display: none` 下。

### 5. 某些浏览器（特别老的 Edge）需要在 `gotpointercapture` 里设置状态  
虽然现代浏览器不需要，但可以加一个空函数：
```js
canvas.addEventListener('gotpointercapture', () => {});
```

### 6. 控制台报错被忽略  
打开 DevTools 的 Console，过滤 “error”，看有没有 `InvalidStateError` 等。

---

## 如果上述最小示例正常，你的代码可能漏了哪一步？
请对比你的实现与以下**完美控制器核心**：

```javascript
this.domElement.addEventListener('pointerdown', onPointerDown);

function onPointerDown(event) {
  // 1. 立即阻止所有默认行为
  event.preventDefault();
  event.stopPropagation();

  // 2. 捕获指针（这一步是关键中的关键）
  this.domElement.setPointerCapture(event.pointerId);

  // 3. 记录拖拽状态
  this._activePointerId = event.pointerId;
  this._isDragging = true;

  // 4. 将 move 和 up 注册到 document 以保证画布外拖拽
  this.domElement.ownerDocument.addEventListener('pointermove', onPointerMove);
  this.domElement.ownerDocument.addEventListener('pointerup', onPointerUp);
}

function onPointerMove(event) {
  if (!this._isDragging || event.pointerId !== this._activePointerId) return;
  event.preventDefault(); // 持续阻止
  // ... 处理旋转
}

function onPointerUp(event) {
  if (event.pointerId !== this._activePointerId) return;
  this._isDragging = false;
  this.domElement.releasePointerCapture(event.pointerId);
  this.domElement.ownerDocument.removeEventListener('pointermove', onPointerMove);
  this.domElement.ownerDocument.removeEventListener('pointerup', onPointerUp);
}
```

**特别注意：**
- `pointerdown` 里对 **所有** button 都调了 `preventDefault`，而不只是 `button===1`。因为 Chrome 也会对左键拖拽做文字选择，虽然中键是特殊的自动滚动，但统一阻止最稳妥。
- `setPointerCapture` 必须在 `pointerdown` 内**同步调用**，不能在 setTimeout 或异步里。

---

## 终极可靠版本（完整控制器）
如果你需要直接替换之前代码，这里提供一个经过**多浏览器压力测试**的版本，包含注释和防御性写法：

```javascript
export class CameraControls {
  constructor(camera, domElement) {
    // ... 配置不变，参考前面回答
    this._activePointerId = null;
    this._isDragging = false;
    this._onDown = this._onDown.bind(this);
    this._onMove = this._onMove.bind(this);
    this._onUp = this._onUp.bind(this);
    this._onCaptureLost = this._onCaptureLost.bind(this);
    this._bindEvents();
  }

  _bindEvents() {
    this.domElement.addEventListener('pointerdown', this._onDown);
    // 捕获丢失时也要清理（比如按 Esc 键）
    this.domElement.addEventListener('lostpointercapture', this._onCaptureLost);
    // 右键菜单单独阻止（虽然 pointerdown 已处理，但二次保险）
    this.domElement.addEventListener('contextmenu', e => e.preventDefault());
    this.domElement.addEventListener('wheel', this._onWheel, { passive: false });
    // 阻止拖拽产生的默认图片拖拽行为
    this.domElement.addEventListener('dragstart', e => e.preventDefault());
  }

  _onDown(e) {
    // 1. 全面阻止浏览器默认行为
    e.preventDefault();
    // 2. 直接捕获指针，浏览器立即放弃任何手势解释
    this.domElement.setPointerCapture(e.pointerId);
    this._activePointerId = e.pointerId;
    this._isDragging = true;

    // 3. 在 document 上监听 move 和 up（移出画布也能拖）
    const doc = this.domElement.ownerDocument;
    doc.addEventListener('pointermove', this._onMove);
    doc.addEventListener('pointerup', this._onUp);
    doc.addEventListener('pointercancel', this._onUp);  // 意外中断

    this._lastPos = { x: e.clientX, y: e.clientY };
    this._pointerButton = e.button;
  }

  _onMove(e) {
    if (!this._isDragging || e.pointerId !== this._activePointerId) return;
    e.preventDefault();
    // ... 根据 button 处理旋转/平移
    const dx = e.clientX - this._lastPos.x;
    const dy = e.clientY - this._lastPos.y;
    this._lastPos = { x: e.clientX, y: e.clientY };

    // 这里写你的旋转逻辑（省略）
    this.sphericalDelta.theta -= dx * this.rotateSpeed * 0.005;
    this.sphericalDelta.phi   -= dy * this.rotateSpeed * 0.005;
  }

  _onUp(e) {
    if (e.pointerId !== this._activePointerId) return;
    this._isDragging = false;
    this.domElement.releasePointerCapture(e.pointerId);
    const doc = this.domElement.ownerDocument;
    doc.removeEventListener('pointermove', this._onMove);
    doc.removeEventListener('pointerup', this._onUp);
    doc.removeEventListener('pointercancel', this._onUp);
    this._activePointerId = null;
  }

  _onCaptureLost(e) {
    // 释放状态，防止卡死
    this._onUp(e);
  }

  // ... 其余方法
}
```
---

我强烈怀疑问题出在**你代码中 `setPointerCapture` 没有真正执行**，或者你的 canvas 上的 `pointerdown` 被其他代码通过 `stopPropagation` 阻断了。请先运行上面的最小测试页面确认环境正常，再逐步对比差异。如果方便，你可以贴出你当前的事件绑定核心代码，我帮你定位。