# Checklist

## 行为惯性机制
- [x] NPC 在 HP≈30% 临界值附近不再出现行为抖动（逃跑↔修炼反复切换）
- [x] 低优先级→高优先级切换不受惯性限制（如日常→生存立即生效）
- [x] 高优先级→低优先级切换需持续满足条件 N 帧后才降级
- [x] 外部强制事件（LLM 指令、被攻击）忽略惯性立即切换
- [x] BehaviorComponent 中新增的惯性字段与现有字段兼容，不破坏序列化
- [x] 3 个 C++ 单元测试全部通过 ✅

## 关系自然衰减
- [x] 长期无互动后 NPC 亲密度按性格相关速率下降
- [x] 正亲密度不低于 0（陌生人保底）
- [x] 负亲密度缓慢回升至 0（敌意自然消解）
- [x] 特殊关系（道侣/师徒）衰减率为普通关系的 50%
- [x] 主动互动后衰减计时器正确重置
- [x] 衰减逻辑不影响现有 32 个测试 ✅

## 分层记忆系统
- [x] 近期记忆保留 20 条完整细节
- [x] 溢出条目正确压缩为摘要存入中期记忆（100条容量）
- [x] 里程碑事件正确写入长期记忆（50条容量）
- [x] LLM prompt 构建按"长期全量 + 中期 TOP10 + 近期全量"注入
- [x] 三层记忆总内存不超过 ~8KB/NPC
- [x] 不影响现有记忆写入逻辑 ✅

## LLM 前线摘要反馈
- [x] LLM 规划 prompt 中包含正确的统计摘要（伤亡率、资源消耗率、任务完成率）
- [x] 摘要格式为可解析的结构化 JSON
- [x] T1 层的 `needs_revision` 标记正确传递到 T0 层下次规划
- [x] T3 NPC 的状态变更纳入摘要统计
- [x] LLM 规划在无前线数据时降级为原行为（不崩溃） ✅

## 瞬时情绪系统
- [x] 情绪值按触发事件正确累积（被辱骂+愤怒、被攻击+恐惧）
- [x] 情绪值每帧正确衰减（0.995 速率）
- [x] 愤怒>阈值触发决斗行为
- [x] 恐惧>阈值触发逃跑行为（即使 HP 正常）
- [x] 生存层（HP<15%）始终覆盖情绪层
- [x] 性格参数正确调节情绪累积速率和触发阈值
- [x] 情绪层正确插入决策循环（生存层之后、指令层之前） ✅

## 行为反思循环
- [x] 连续 3 次采矿低产出后，采矿权重降低
- [x] 连续 2 次负面社交后，对该对象的互动概率降低
- [x] 连续 3 次探索某区域无收获后，转换探索区域
- [x] 替代行为成功后，衰减因子逐步恢复
- [x] 反思数据不影响序列化和持久化 ✅

## 信息传播系统
- [x] 目击事件在八卦/闲聊中按规则传播
- [x] 亲密度<30 或谨慎>60 的 NPC 不传播敏感信息
- [x] 信息在传播 2 次以上后出现正确扭曲
- [x] 流言到达当事人后触发关系变化
- [x] 每帧信息传播事件不超过配置上限
- [x] 传播系统不额外遍历社交网络（仅在已有互动中计算） ✅

## 整体回归
- [x] 现有 32 个 C++ 单元测试全部通过（扩展至 68 个）
- [x] 6 层优先级决策流程保持正确
- [x] WASM 编译成功，无新增编译警告
- [x] NPC 内存占用增幅在可接受范围（< 2× 当前值） ✅
