- [x] 项目中已成功安装 `@react-three/fiber` 和 `three` 相关依赖包。
- [x] 游戏主界面已替换为 Three.js 驱动的 `<Canvas>` 渲染，且默认视角为正交相机（OrthographicCamera）的等距 2.5D 视角。
- [x] 玩家、NPC、都城和资源点以 3D 几何体（如 BoxGeometry / CylinderGeometry）形式被正确渲染在场景中。
- [x] 地图实体拥有正确的光影反馈（如受到方向光照射，产生并接收阴影）。
- [x] NPC 和玩家头顶的状态面板（名称、血量、活动状态）通过 `drei` 的 `<Html>` 组件正确悬浮在 3D 模型上方，不随视角扭曲。
- [x] 玩家可以通过点击 3D 的 NPC 模型触发原有的交互面板，原有的 W/A/S/D 移动逻辑正常生效。
- [x] 根据 `appearance.ts` 的规则，高境界或特殊体质的 NPC/玩家拥有发光效果（如通过发光材质或附加的光环平面表现）。
