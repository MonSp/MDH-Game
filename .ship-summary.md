# PR: Phase 2.3 — Pixel Panel Modal Frames & Minimap

**Branch:** `feat/pixel-panel-modals`
**Base:** `main`
**Version:** 1.13.0.1

## Summary

Pixel art style upgrade: decorative PixelPanel frame applied to all 12 modal panels, new PixelMinimap and PixelTechniqueIcon components, and procedurally-generated portrait data URLs for the HUD avatar.

### Files Changed

| Category | Files | Change |
|---|---|---|
| New | `PixelPanel.tsx` | Decorative frame with golden corner ornaments, inner border glow, scan-line overlay |
| New | `PixelMinimap.tsx` | Canvas2D pixel minimap rendering terrain + entities |
| New | `PixelTechniqueIcon.tsx` | React wrapper for technique icon data URLs |
| Modified | `pixelSpriteGenerator.ts` | +201 lines — `getCharacterPortraitDataURL` for HUD avatar |
| Modified | 12 panel files | Wrapped in `<PixelPanel>` for consistent decorative frame |
| Modified | VERSION, CHANGELOG, TODOS, package.json | Bump 1.13.0.0 → 1.13.0.1 |

## Test Coverage

```
Test Files  30 passed (30)
     Tests  666 passed (666)
```

## Verification

- [x] `npm run build` — TypeScript compiles cleanly
- [x] `npx vitest run` — 666 tests pass across 30 test files
- [x] VERSION bumped: 1.13.0.0 → 1.13.0.1
- [x] Branch pushed to origin

## Notes

- Remote is a local-only git repo (`file:///home/test/MyGame.git`) — no GitHub/GitLab PR can be created
- To merge: `git checkout main && git merge feat/pixel-panel-modals`
