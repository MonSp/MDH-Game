# 经济系统代码设计

## 1. 数据模型

### 1.1 货币类型

```typescript
enum CurrencyType {
  SpiritStone = 'spirit_stone'  // 灵石
}
```

### 1.2 交易类型

```typescript
enum TransactionType {
  Gain = 'gain',
  Spend = 'spend',
  Transfer = 'transfer'
}
```

### 1.3 物品接口

```typescript
interface Item {
  id: string;
  name: string;
  type: ItemType;
  quality: ItemQuality;
  price: number;
  description?: string;
}

enum ItemType {
  Material = 'material',
  Pill = 'pill',
  Equipment = 'equipment',
  Quest = 'quest'
}

enum ItemQuality {
  Common = 1,    // 普通
  Good = 2,      // 良品
  Excellent = 3, // 极品
  Legendary = 4  // 传说
}
```

## 2. 核心类设计

### 2.1 EconomyService

```typescript
class EconomyService {
  private static instance: EconomyService;

  static getInstance(): EconomyService {
    if (!EconomyService.instance) {
      EconomyService.instance = new EconomyService();
    }
    return EconomyService.instance;
  }

  addCurrency(playerId: string, type: CurrencyType, amount: number): void {
    const player = PlayerService.getPlayer(playerId);
    player.spiritStones += amount;
    this.recordTransaction(playerId, TransactionType.Gain, type, amount);
    EventBus.emit(EconomyEvent.CURRENCY_CHANGED, { playerId, type, amount, balance: player.spiritStones });
  }

  spendCurrency(playerId: string, type: CurrencyType, amount: number): boolean {
    const player = PlayerService.getPlayer(playerId);
    if (player.spiritStones < amount) {
      return false;
    }
    player.spiritStones -= amount;
    this.recordTransaction(playerId, TransactionType.Spend, type, amount);
    EventBus.emit(EconomyEvent.CURRENCY_CHANGED, { playerId, type, amount, balance: player.spiritStones });
    return true;
  }

  transferCurrency(fromPlayerId: string, toPlayerId: string, type: CurrencyType, amount: number): boolean {
    if (!this.spendCurrency(fromPlayerId, type, amount)) {
      return false;
    }
    this.addCurrency(toPlayerId, type, amount);
    this.recordTransaction(fromPlayerId, TransactionType.Transfer, type, amount, toPlayerId);
    return true;
  }

  calculateCraftCost(basePrice: number, countryTrait?: { type: string; value: number }): number {
    if (countryTrait?.type === 'craft_cost') {
      return Math.floor(basePrice * (1 - countryTrait.value / 100));
    }
    return basePrice;
  }

  private recordTransaction(playerId: string, type: TransactionType, currency: CurrencyType, amount: number, toPlayerId?: string): void {
    TransactionService.record({
      playerId,
      type,
      currencyType: currency,
      amount,
      targetPlayerId: toPlayerId,
      timestamp: Date.now()
    });
  }
}
```

### 2.2 ItemService

```typescript
class ItemService {
  private static instance: ItemService;
  private itemDatabase: Map<string, Item>;
  private playerItems: Map<string, Map<string, number>>;

  static getInstance(): ItemService {
    if (!ItemService.instance) {
      ItemService.instance = new ItemService();
    }
    return ItemService.instance;
  }

  addItem(playerId: string, itemId: string, count: number = 1): void {
    if (!this.playerItems.has(playerId)) {
      this.playerItems.set(playerId, new Map());
    }
    const items = this.playerItems.get(playerId)!;
    items.set(itemId, (items.get(itemId) || 0) + count);
    EventBus.emit(ItemEvent.ITEM_ADDED, { playerId, itemId, count });
  }

  removeItem(playerId: string, itemId: string, count: number = 1): boolean {
    const items = this.playerItems.get(playerId);
    if (!items) return false;
    const currentCount = items.get(itemId) || 0;
    if (currentCount < count) return false;
    if (currentCount === count) {
      items.delete(itemId);
    } else {
      items.set(itemId, currentCount - count);
    }
    EventBus.emit(ItemEvent.ITEM_REMOVED, { playerId, itemId, count });
    return true;
  }

  getItemCount(playerId: string, itemId: string): number {
    return this.playerItems.get(playerId)?.get(itemId) || 0;
  }

  hasItem(playerId: string, itemId: string): boolean {
    return this.getItemCount(playerId, itemId) > 0;
  }

  getPlayerItems(playerId: string): Array<{ item: Item; count: number }> {
    const items = this.playerItems.get(playerId);
    if (!items) return [];
    return Array.from(items.entries()).map(([itemId, count]) => ({
      item: this.itemDatabase.get(itemId)!,
      count
    }));
  }
}
```

### 2.3 MarketService

```typescript
class MarketService {
  private static instance: MarketService;

  static getInstance(): MarketService {
    if (!MarketService.instance) {
      MarketService.instance = new MarketService();
    }
    return MarketService.instance;
  }

  purchaseItem(buyerId: string, sellerId: string, itemId: string, price: number): PurchaseResult {
    if (!EconomyService.getInstance().spendCurrency(buyerId, CurrencyType.SpiritStone, price)) {
      return { success: false, reason: 'insufficient_funds' };
    }

    if (!ItemService.getInstance().hasItem(sellerId, itemId)) {
      EconomyService.getInstance().addCurrency(buyerId, CurrencyType.SpiritStone, price);
      return { success: false, reason: 'item_not_available' };
    }

    ItemService.getInstance().removeItem(sellerId, itemId);
    ItemService.getInstance().addItem(buyerId, itemId);
    EconomyService.getInstance().addCurrency(sellerId, CurrencyType.SpiritStone, price);

    return { success: true };
  }

  sellToNPC(playerId: string, itemId: string, npcId: string): boolean {
    const config = ITEM_PRICE_CONFIG[itemId];
    if (!config) return false;

    const sellPrice = Math.floor(config.price * 0.5);
    ItemService.getInstance().removeItem(playerId, itemId);
    EconomyService.getInstance().addCurrency(playerId, CurrencyType.SpiritStone, sellPrice);
    return true;
  }
}
```

## 3. 数据库设计

### 3.1 交易记录表

```sql
CREATE TABLE transactions (
  id TEXT PRIMARY KEY,
  player_id TEXT NOT NULL,
  type TEXT NOT NULL,
  currency_type TEXT NOT NULL,
  amount INTEGER NOT NULL,
  target_player_id TEXT,
  timestamp INTEGER NOT NULL
);

CREATE INDEX idx_transactions_player ON transactions(player_id);
```

### 3.2 物品表

```sql
CREATE TABLE items (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  type TEXT NOT NULL,
  quality INTEGER NOT NULL,
  price INTEGER NOT NULL,
  description TEXT
);
```

### 3.3 玩家物品表

```sql
CREATE TABLE player_items (
  player_id TEXT NOT NULL,
  item_id TEXT NOT NULL,
  count INTEGER NOT NULL DEFAULT 1,
  PRIMARY KEY (player_id, item_id)
);
```

## 4. 事件定义

```typescript
enum EconomyEvent {
  CURRENCY_CHANGED = 'economy:currency_changed',
  TRANSACTION_COMPLETED = 'economy:transaction',
  PURCHASE_COMPLETED = 'economy:purchase',
  SALE_COMPLETED = 'economy:sale'
}

enum ItemEvent {
  ITEM_ADDED = 'item:added',
  ITEM_REMOVED = 'item:removed'
}
```

## 5. 使用示例

```typescript
const economyService = EconomyService.getInstance();
const itemService = ItemService.getInstance();

economyService.addCurrency(playerId, CurrencyType.SpiritStone, 1000);

const canCraft = economyService.spendCurrency(playerId, CurrencyType.SpiritStone, 500);

itemService.addItem(playerId, 'WashMarrowPill', 1);

EventBus.on(EconomyEvent.CURRENCY_CHANGED, ({ playerId, balance }) => {
  UpdatePlayerUI(playerId, { spiritStones: balance });
});
```

---

## 6. LLM决策集成

### 6.1 经济战略LLM调用

```typescript
// server/game/services/EconomyStrategyLLM.ts

interface EconomyStrategyContext {
  family_id: string;
  family_treasury: {
    total: number;
    liquid: number;
    locked: number;
  };
  market_status: {
    spirit_stone_price: number;
    xi_sui_dan_price: number;
    recent_trends: string;
  };
  player_economy: {
    player_wealthiest: number;
    total_players_wealth: number;
  };
}

class EconomyStrategyLLM {
  async generateStrategy(context: EconomyStrategyContext): Promise<EconomyStrategyPlan> {
    const prompt = this.buildStrategyPrompt(context);
    const response = await LLMGatewayService.getInstance().call(prompt);
    return JSON.parse(response);
  }

  private buildStrategyPrompt(context: EconomyStrategyContext): string {
    return `
      你是一个修仙世界的家族家主，负责制定家族经济战略。
      家族库房：总资产${context.family_treasury.total}灵石，流动资金${context.family_treasury.liquid}灵石。
      市场状态：洗髓丹价格${context.market_status.xi_sui_dan_price}灵石，
      趋势：${context.market_status.recent_trends}
      请生成7天的经济战略计划，包括市场操作、贸易路线、资源分配等。
      返回JSON格式的战略计划。
    `;
  }
}

interface EconomyStrategyPlan {
  plan_id: string;
  horizon_days: number;
  primary_goal: string;
  sub_tasks: Array<{
    task_id: number;
    description: string;
    priority: number;
    target_completion_day: number;
    action_type: 'MARKET_SELL' | 'TRADE_CARAVAN' | 'FAMILY_SUBSIDY';
    action_params: Record<string, any>;
  }>;
}
```

---

## 7. 实现状态 (2026-04)

### 已实现
- `EconomyService` (addCurrency/spendCurrency/transferCurrency)
- `ItemService` (8个预定义物品, 4品质4类型)
- `MarketService` (purchaseItem玩家间交易, sellToNPC半价出售)
- `calculateCraftCost` (应用craft_cost国家特质)

### Bug
- **双重货币存储**: `EconomyService.playerCurrency` Map 与 `Player.spiritStones` 字段各自独立，未同步
- `sellToNPC` 未检查NPC是否存在或有钱
- `purchaseItem` 未防止自买自卖

### 未实现
- **`EconomyStrategyLLM`**: 未实现
- 拍卖行/列表市场未实现
- 动态定价/供需系统未实现(vs docs设计中的战事经济学)
- 交易记录(transactions表)创建但从未写入
- 物品耐久/装备降解未实现

### 与 docs/ 设计差距
- docs设计中的7种战略资源(粮/铁/灵石/灵兽/灵药/兵器/阵法材料)被简化为单一灵石货币
- docs中的战争经济学(军费消耗/战时通胀/制裁)完全未实现
- docs中的三层经济AI(国家战略→区域战术→城市运营)未实现
