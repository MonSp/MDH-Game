# 代码架构设计

## 1. 技术选型

### 1.1 前端技术栈

| 层级 | 技术栈 | 说明 |
|------|--------|------|
| 游戏引擎 | Phaser 3.x | 2.5D像素游戏渲染 |
| 前端框架 | React/Vue | UI界面 |
| 实时通信 | Socket.IO Client | WebSocket通信 |

### 1.2 后端技术栈（混合架构）

| 层级 | 技术栈 | 说明 |
|------|--------|------|
| 网关层 | Node.js + Express | HTTP服务、WebSocket网关 |
| 游戏逻辑层 | C++ ECS Engine | NPC计算密集型逻辑 |
| 实时通信 | Socket.IO | WebSocket实时通信 |
| 数据库 | SQLite | 轻量级本地数据库 |
| IPC通信 | Unix Socket | Node.js与C++进程间通信 |
| 类型安全 | TypeScript/C++ | 前后端类型安全 |

### 1.3 C++ ECS架构

| 模块 | 技术栈 | 说明 |
|------|--------|------|
| ECS框架 | 自研轻量级ECS | Entity-Component-System模式 |
| 组件存储 | Archetype | 组件按类型批量存储优化 |
| 线程调度 | Job System | 多线程并行计算 |
| 任务分发 | ThreadPool | 可配置4-16线程 |
| 工作窃取 | Work Stealing | 负载均衡算法 |

### 1.4 LLM大模型服务

| 模块 | 技术栈 | 说明 |
|------|--------|------|
| HTTP客户端 | libcurl | 异步HTTP请求，支持OpenAI/本地LLM |
| 多线程 | ThreadPool | 8-16并发请求 |
| 提示词 | LLMPromptBuilder | Tier差异化提示词构建 |
| 规划客户端 | LLMPlanningClient | NPC规划请求管理 |

## 2. 项目目录结构

```
src/
├── client/                      # 前端代码
│   ├── scenes/                  # Phaser游戏场景
│   ├── entities/                # 游戏实体（玩家、NPC、资源点）
│   ├── ui/                     # UI组件
│   └── network/                 # 网络通信
├── server/                      # 后端代码
│   ├── main.cpp                # C++引擎入口
│   ├── CMakeLists.txt          # CMake构建配置
│   ├── game/                   # 游戏逻辑
│   │   ├── ecs/                # ECS核心架构
│   │   │   ├── Component.h     # 组件基类
│   │   │   ├── Entity.h        # 实体定义
│   │   │   ├── Registry.h      # 实体注册表
│   │   │   ├── Archetype.h     # Archetype存储
│   │   │   ├── components/     # 组件定义
│   │   │   │   ├── PositionComponent.h     # 位置组件
│   │   │   │   ├── StatsComponent.h        # 属性组件
│   │   │   │   ├── BehaviorComponent.h     # 行为组件
│   │   │   │   ├── PersonalityComponent.h  # 人格组件
│   │   │   │   ├── IdentityComponent.h     # 身份组件
│   │   │   │   ├── LifecycleComponent.h    # 生命周期组件
│   │   │   │   └── ResourcesComponent.h    # 资源组件
│   │   │   └── systems/         # 系统实现
│   │   │       ├── NPChunkUpdateSystem.h    # NPC分片更新
│   │   │       ├── LifecycleSystem.h        # 生死轮回系统
│   │   │       ├── PopulationBalanceSystem.h # 人口平衡系统
│   │   │       ├── LLMPlanningSystem.h     # LLM规划系统
│   │   │       └── WorldUpdateLoop.h        # 主循环
│   │   ├── job/                 # Job System
│   │   │   ├── TaskQueue.h     # 无锁任务队列
│   │   │   ├── Job.h           # Job基类
│   │   │   ├── ThreadPool.h    # 线程池
│   │   │   └── JobDispatcher.h # 任务分发器
│   │   ├── npc/                 # NPC系统
│   │   │   ├── NPCCreationSystem.h  # NPC创建
│   │   │   ├── BehaviorTreeSystem.h  # 行为树
│   │   │   ├── MovementSystem.h     # 移动系统
│   │   │   └── CombatSystem.h      # 战斗系统
│   │   ├── llm/                 # LLM大模型服务
│   │   │   ├── LLMHttpClient.h  # HTTP客户端(libcurl)
│   │   │   ├── LLMService.h      # LLM服务
│   │   │   ├── LLMPlanningClient.h # 规划客户端
│   │   │   └── LLMPromptBuilder.h # 提示词构建
│   │   └── ipc/                 # IPC通信层
│   │       ├── Protocol.h       # 消息协议
│   │       ├── UnixSocketServer.h  # Unix Socket服务器
│   │       ├── MessageQueue.h   # 消息队列
│   │       └── LLMPlanningIPC.h  # LLM IPC通信
│   ├── addons/                  # Node.js Addon
│   │   └── NPAddon.h           # C++引擎桥接
│   ├── services/                # 业务服务(TypeScript)
│   ├── models/                  # 数据模型
│   └── network/                 # 网络处理
├── shared/                      # 共享代码
│   ├── types/                   # 类型定义
│   └── constants/               # 常量定义
└── config/                      # 配置文件
```

## 3. 核心模块架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Client                                │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                     │
│  │ Scene   │  │ Entity  │  │  UI     │                     │
│  │ Manager │  │ Manager │  │ Manager │                     │
│  └────┬────┘  └────┬────┘  └────┬────┘                     │
│       └───────────┼────────────┘                            │
│                   │ Socket.IO                                │
└───────────────────┼─────────────────────────────────────────┘
                    │
┌───────────────────┼─────────────────────────────────────────┐
│                   │        Node.js Gateway                   │
│       ┌───────────┼───────────┐                             │
│       │  Express  │  Socket.IO │                            │
│       │  HTTP     │  Server    │                            │
│       └───────────┴───────────┴────┘                        │
└───────────────────┼───────────────────────────────────────────┘
                    │
┌───────────────────┼─────────────────────────────────────────┐
│                   │      C++ ECS Game Engine                  │
│  ┌────────────────┴────────────────┐                        │
│  │         Job System               │                        │
│  │  ThreadPool (4-16线程) + TaskQueue                      │
│  └────────────────┬────────────────┘                        │
│                   │                                           │
│  ┌────────────────┼────────────────┐                        │
│  │     NPC Systems (并行)          │                        │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │  │NPChunk  │ │Lifecycle│ │PopBal   │ │   LLM   │       │
│  │  │Update   │ │System   │ │System    │ │Planning │       │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │
│  └────────────────┬────────────────┘                        │
│                   │                                           │
│  ┌────────────────┼────────────────┐                        │
│  │   ECS Registry + LLMComponent    │                        │
│  └─────────────────────────────────┘                        │
│                                                               │
│  ┌─────────────────────────────────┐                        │
│  │   LLM Service (内置HTTP客户端)    │                        │
│  │   - LLMHttpClient (libcurl)      │                        │
│  │   - 8-16并发请求                 │                        │
│  │   - OpenAI / 本地LLM             │                        │
│  └─────────────────────────────────┘                        │
└─────────────────────────────────────────────────────────────┘
```

## 4. 代码设计文档索引

| 编号 | 文档名称 | 对应体系 |
|------|----------|----------|
| [01-战国七国体系代码设计](01-战国七国体系代码设计.md) | 战国七国体系 | 战国七国设定 |
| [02-家族势力体系代码设计](02-家族势力体系代码设计.md) | 家族势力体系 | 家族势力体系 |
| [03-修仙境界体系代码设计](03-修仙境界体系代码设计.md) | 修仙境界体系 | 修仙境界体系 |
| [04-地图资源系统代码设计](04-地图资源系统代码设计.md) | 地图资源系统 | 地图资源系统 |
| [05-经济系统代码设计](05-经济系统代码设计.md) | 经济系统 | 经济系统设计 |
| [06-NPC行为与生命周期代码设计](06-NPC行为与生命周期代码设计.md) | NPC行为与生命周期 | NPC行为与生命周期 |
| [07-玩家系统代码设计](07-玩家系统代码设计.md) | 玩家系统 | 玩家系统设计 |
| [11-NPC生死轮回与人口平衡代码设计](11-NPC生死轮回与人口平衡代码设计.md) | NPC生死轮回与人口平衡 | NPC生死轮回与人口平衡 |

### 4.1 C++ ECS架构专题

| 文档 | 说明 |
|------|------|
| [C++ ECS架构设计](../src/server/game/ecs/) | ECS核心组件、Registry、Archetype |
| [Job System设计](../src/server/game/job/) | 线程池、任务队列、工作窃取 |
| [NPC组件设计](../src/server/game/ecs/components/) | 7个NPC核心组件 |
| [NPC系统设计](../src/server/game/npc/) | 行为树、移动、战斗系统 |
| [IPC通信设计](../src/server/game/ipc/) | 消息协议、Unix Socket |

## 5. 数据流设计

### 5.1 整体数据流

```
User Input → Client Scene → Socket.IO → Node.js Gateway → C++ ECS Engine
                                    ↑                    │
                                    └──── Event ─────────┘
```

### 5.2 IPC数据流

```
┌─────────────────┐         ┌─────────────────┐         ┌─────────────────┐
│   Node.js       │         │  Unix Socket   │         │  C++ Engine    │
│   Gateway       │ ──────▶ │   IPC Layer    │ ──────▶ │                │
│                 │         │                 │         │                │
│ - Express HTTP  │         │ - MessageQueue │         │ - JobSystem   │
│ - Socket.IO    │         │ - Protocol     │         │ - NPC Systems │
│ - Addon Bridge │ ◀────── │                 │ ◀────── │ - ECS Registry│
└─────────────────┘         └─────────────────┘         └─────────────────┘
```

### 5.3 NPC更新数据流

```
每帧更新 (16ms):
┌──────────────────────────────────────────────────────────────┐
│  WorldUpdateLoop::updateOnce()                               │
│                                                              │
│  1. NPChunkUpdateSystem::updateAllNPCs()                    │
│     └─▶ JobDispatcher::dispatchParallel()                    │
│         ├─▶ Thread 0: Job (NPC 0-999)                      │
│         │   └─▶ BehaviorTreeSystem::evaluate()              │
│         │   └─▶ BehaviorTreeSystem::execute()               │
│         │   └─▶ MovementSystem::update()                    │
│         ├─▶ Thread 1: Job (NPC 1000-1999)                  │
│         ...                                                  │
│         └─▶ Thread N: Job (NPC N*1000 ...)                  │
│                                                              │
│  2. LifecycleSystem::updateAllNPCs()                        │
│     └─▶ 寿元检查 + 死亡判定                                  │
│                                                              │
│  3. PopulationBalanceSystem::checkAndTriggerBirth()         │
│     └─▶ 人口缺口检测 + 新生NPC触发                           │
└──────────────────────────────────────────────────────────────┘
```

## 6. 关键设计模式

### 6.1 C++ ECS架构模式

- **Entity-Component-System (ECS)**: NPC数据与逻辑分离，组件可组合
- **Archetype存储**: 相同组件类型的Entity批量存储，优化缓存命中
- **Job System**: 多线程任务并行，避免锁竞争

### 6.2 TypeScript设计模式

- **单例模式**: GameService、DataService 全局唯一
- **观察者模式**: 事件系统解耦实体间通信
- **状态机模式**: 玩家状态、NPC AI状态管理
- **对象池模式**: 资源点、子弹等频繁创建销毁的对象

### 6.3 混合架构模式

- **IPC通信模式**: Node.js与C++通过Unix Socket进行进程间通信
- **主从分离模式**: Node.js处理I/O，C++处理计算密集型任务

---

## 7. 性能指标

### 7.1 性能目标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 10万NPC单帧更新 | < 16ms | 60 FPS目标 |
| 最大NPC数量 | 100,000+ | 全服实时计算 |
| 线程数配置 | 4-16 | 可根据CPU核心数配置 |
| 内存占用 | < 2GB | 10万NPC基准 |
| Job Queue响应 | < 1ms | 任务调度延迟 |

### 7.2 线程池配置

```cpp
struct ThreadPoolConfig {
    uint32_t threadCount;      // 建议: std::thread::hardware_concurrency() - 1
    uint32_t queueSize;        // 任务队列上限
    bool enableStealing;        // 工作窃取启用
};
```

### 7.3 NPC分片配置

| 配置项 | 值 | 说明 |
|--------|-----|------|
| CHUNK_SIZE | 1000 | 每片NPC数量 |
| 最大Job数 | 100 | 10万NPC = 100个Job |
| 并行度 | 8-16 | 线程池大小 |

---

## 8. LLM决策服务架构

### 8.1 服务架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      LLM决策服务层                           │
│  ┌─────────────────┐  ┌─────────────────┐                 │
│  │  OpenAI API     │  │  本地LLM模型    │                 │
│  │  (GPT-4/3.5)   │  │  (Llama/ChatGLM)│                 │
│  └────────┬────────┘  └────────┬────────┘                 │
│           │                     │                           │
│           └──────────┬───────────┘                           │
│                      ▼                                      │
│           ┌───────────────────┐                            │
│           │   LLM网关服务      │                            │
│           │ (负载均衡/降级/缓存)│                           │
│           └─────────┬─────────┘                            │
│                     │                                       │
└─────────────────────┼───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                    游戏逻辑服务层                            │
│  ┌─────────────────┐  ┌─────────────────┐                 │
│  │  NPC行为树引擎  │  │  LLM规划模块    │                 │
│  │                 │  │  (计划生成/执行) │                 │
│  └────────┬────────┘  └────────┬────────┘                 │
│           │                     │                           │
│           └──────────┬──────────┘                           │
│                      ▼                                      │
│           ┌───────────────────┐                            │
│           │   事件调度器       │                            │
│           │ (触发LLM调用/更新) │                            │
│           └───────────────────┘                            │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 项目目录结构扩展

```
src/
├── client/                 # 前端代码
│   ├── scenes/            # Phaser游戏场景
│   ├── entities/          # 游戏实体（玩家、NPC、资源点）
│   ├── ui/               # UI组件
│   └── network/           # 网络通信
├── server/                # 后端代码
│   ├── game/             # 游戏逻辑
│   │   ├── services/     # 业务服务
│   │   │   ├── LLMPlanningService.ts    # LLM规划服务
│   │   │   ├── LLMGatewayService.ts     # LLM网关服务
│   │   │   ├── NPCBehaviorTree.ts       # NPC行为树
│   │   │   └── ...
│   │   └── modules/      # 功能模块
│   │       ├── NPC/
│   │       ├── Player/
│   │       ├── Family/
│   │       └── World/
│   ├── models/           # 数据模型
│   ├── services/         # 业务服务
│   └── network/         # 网络处理
├── shared/               # 共享代码
│   ├── types/           # 类型定义
│   │   └── LLMPlanning.ts  # LLM规划相关类型
│   └── constants/       # 常量定义
└── config/              # 配置文件
```

### 7.3 LLM规划核心类型定义

```typescript
// shared/types/LLMPlanning.ts

export enum LLMTier {
  T0 = 'T0', // 天道级（皇帝、太虚境霸主）
  T1 = 'T1', // 战略级（家族家主、大将军）
  T2 = 'T2', // 战术级（长老、金丹期核心子弟）
  T3 = 'T3'  // 行为树级（其余NPC）
}

export enum PlanningType {
  NORMAL = 'NORMAL',
  EMERGENCY = 'EMERGENCY'
}

export enum ActionType {
  // 通用
  IDLE = 'IDLE',
  REST = 'REST',
  PATROL = 'PATROL',
  EXPLORE = 'EXPLORE',
  // 家族职责
  CULTIVATE = 'CULTIVATE',
  TRADE = 'TRADE',
  LOGISTICS = 'LOGISTICS',
  // 战略
  MILITARY_ORDER = 'MILITARY_ORDER',
  DIPLOMACY = 'DIPLOMACY',
  INTELLIGENCE = 'INTELLIGENCE',
  // 资源
  RESOURCE_ALLOCATION = 'RESOURCE_ALLOCATION',
  RESOURCE_PURCHASE = 'RESOURCE_PURCHASE',
  RESOURCE_RAID = 'RESOURCE_RAID',
  CAPTURE_RESOURCE_POINT = 'CAPTURE_RESOURCE_POINT',
  // 高层
  DOMAIN_WAR = 'DOMAIN_WAR',
  ALLIANCE_FORMATION = 'ALLIANCE_FORMATION',
  CULTIVATE_BREAKTHROUGH = 'CULTIVATE_BREAKTHROUGH'
}

export interface LLMPlanningRequest {
  npc_id: string;
  npc_data: NPCData;
  world_context: WorldContext;
  planning_horizon: '1天' | '1周' | '1月';
  planning_type: PlanningType;
}

export interface LLMPlanningResponse {
  plan_id: string;
  nppc_id: string;
  horizon_days: number;
  primary_goal: string;
  sub_tasks: SubTask[];
  fallback_behavior: string;
}

export interface SubTask {
  task_id: number;
  description: string;
  priority: number;
  target_completion_day: number;
  action_type: ActionType;
  action_params: Record<string, any>;
}

export interface LLMPlan {
  plan_id: string;
  generated_at: number;
  expires_at: number;
  tasks: SubTask[];
  current_task_index: number;
  status: 'ACTIVE' | 'COMPLETED' | 'INTERRUPTED' | 'FAILED';
}

export interface NPCWithLLM extends NPCBaseAttributes {
  llm_plan?: LLMPlan;
  llm_eligibility: {
    tier: LLMTier;
    last_planning_time: number;
    planning_horizon: string;
  };
}
```

### 7.4 LLM服务配置

```typescript
// server/config/LLMConfig.ts

interface LLMServiceConfig {
  provider: 'openai' | 'local';
  model: string;
  temperature: number;
  max_tokens: number;
  cache_enabled: boolean;
  fallback_script_library: string;
  tier_config: {
    [LLMTier.T0]: { frequency: '月/次', horizon: '1月' };
    [LLMTier.T1]: { frequency: '周/次', horizon: '1周' };
    [LLMTier.T2]: { frequency: '日/次', horizon: '1天' };
  };
  emergency_config: {
    response_time_limit: number; // 毫秒
    max_tasks: number;
  };
}

const LLM_SERVICE_CONFIG: LLMServiceConfig = {
  provider: 'openai',
  model: 'gpt-4',
  temperature: 0.7,
  max_tokens: 2000,
  cache_enabled: true,
  fallback_script_library: 'scripts/llm_fallback/',
  tier_config: {
    [LLMTier.T0]: { frequency: '月/次', horizon: '1月' },
    [LLMTier.T1]: { frequency: '周/次', horizon: '1周' },
    [LLMTier.T2]: { frequency: '日/次', horizon: '1天' }
  },
  emergency_config: {
    response_time_limit: 5 * 60 * 1000,
    max_tasks: 3
  }
};
```

### 7.5 与现有系统的集成

| 模块 | 集成方式 | 数据流向 |
|:---|:---|:---|
| **NPC行为树** | LLM任务→行为树节点翻译 | LLM → BT |
| **世界状态** | 读取外交/战争/资源状态 | World → LLM |
| **家族系统** | 读取/更新家族状态 | Family ↔ LLM |
| **经济系统** | 触发市场/跑商事件 | Economy ↔ LLM |
| **事件系统** | 接收/发布世界事件 | Event ↔ LLM |
